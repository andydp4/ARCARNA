# Example crontab entries for ARCARNA EPOS on the production VPS.
# Install: crontab -e  (adjust paths to match your deploy dir)

# Nightly Neon backup → R2 (02:15 UTC)
15 2 * * * /root/ARCARNA/scripts/backup-neon-to-r2.sh >> /var/log/arcarna-backup.log 2>&1
