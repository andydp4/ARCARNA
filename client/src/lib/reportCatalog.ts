/**
 * The Evidence catalogue lives in shared/ so the server can check a Truths at
 * a glance layout against it (v1.2 Phase 3). Re-exported here for the pages
 * that already import it from the client.
 */
export * from "@shared/evidenceCatalog";
