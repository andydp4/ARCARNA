/** User-facing brand identity (ARCARNA rebrand). */
export const BRAND_NAME = "ARCARNA";
export const BRAND_PRODUCT_NAME = "ARCARNA EPOS";

/** Default public mount path (see VITE_BASE_PATH / APP_BASE_PATH). */
export const DEFAULT_APP_BASE_PATH = "/arcarna";

/** Previous mount path — keep redirects for bookmarks. */
export const LEGACY_APP_BASE_PATH = "/midnight";

/** A icon — favicon, PWA icons, collapsed nav. */
export const BRAND_MARK = "/brand/arcarna-mark.png";

/** Full wordmark — auth shells, portal, expanded sidebar. */
export const BRAND_WORDMARK = "/brand/arcarna-wordmark.png";
