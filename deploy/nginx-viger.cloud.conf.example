# Example Nginx site for viger.cloud — copy to /etc/nginx/sites-available/viger.cloud
# Required for large contact (.vcf) and spreadsheet imports (default limit is 1MB → HTTP 413).

server {
    listen 80;
    server_name viger.cloud www.viger.cloud;

    client_max_body_size 25m;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
